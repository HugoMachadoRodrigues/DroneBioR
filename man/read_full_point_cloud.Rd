% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/full_pointcloud.R
\name{read_full_point_cloud}
\alias{read_full_point_cloud}
\title{Read a full-resolution LAS/LAZ/COPC point cloud}
\usage{
read_full_point_cloud(
  path,
  roi_polygon = NULL,
  max_points = Inf,
  chunk_size = 1e+05
)
}
\arguments{
\item{path}{Path to \code{.las}, \code{.laz} or \code{.copc.laz}.}

\item{roi_polygon}{Optional polygon ROI with \code{x} and \code{y} columns.}

\item{max_points}{Maximum number of points to return when no ROI is supplied.}

\item{chunk_size}{Number of point records per scan chunk for LAS files.}
}
\value{
Data frame with full-resolution point coordinates inside the ROI.
}
\description{
The function prefers an uncompressed LAS file because it can be read with the
package's built-in reader. If a LAZ or COPC LAZ path is supplied, the function
first looks for the corresponding ODM \code{.las} sidecar. If no LAS sidecar is
available, it attempts to use an installed external reader such as \code{lidR}.
}
\examples{
\dontrun{
pc <- read_full_point_cloud("dense.laz")
}
}
