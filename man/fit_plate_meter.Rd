% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biomass_mapping.R
\name{fit_plate_meter}
\alias{fit_plate_meter}
\title{Calibrate a rising plate / disc meter against clipped biomass}
\usage{
fit_plate_meter(
  data,
  height = "plate_height_cm",
  biomass = "biomass_kgha",
  group = NULL,
  min_n = 5L
)
}
\arguments{
\item{data}{Data frame with a height column and a biomass column. Rows
missing either are dropped from the fit.}

\item{height}{Name of the compressed-height column (e.g. \code{plate_height_cm}).}

\item{biomass}{Name of the dry-matter biomass column (\code{biomass_kgha}).}

\item{group}{Optional factor column (e.g. \code{pasture}) added as a fixed
effect. \code{NULL} pools all points into one equation.}

\item{min_n}{Minimum number of complete clip points required.}
}
\value{
An object of class \code{dronebio_plate_meter}: the \code{lm}, the coefficient
table, leave-one-out CV metrics and the column names used.
}
\description{
Fits the classic forage calibration \code{biomass = a + b * height} (optionally
with a pasture/species factor) from the small set of clipped quadrats that
carry both a compressed plate height and a measured dry-matter biomass.
The fitted object then predicts biomass for the many plate-only points,
multiplying the effective calibration sample before the drone model is
built - the double-sampling idea behind Page et al. (2025).
}
\examples{
set.seed(1)
h <- runif(12, 2, 18)
d <- data.frame(plate_height_cm = h,
                biomass_kgha = 200 + 320 * h + rnorm(12, sd = 150))
cal <- fit_plate_meter(d)
cal$metrics$r2
}
