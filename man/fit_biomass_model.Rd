% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biomass_mapping.R
\name{fit_biomass_model}
\alias{fit_biomass_model}
\title{Fit a field-calibrated biomass model (staged LM / random forest)}
\usage{
fit_biomass_model(
  data,
  response = "biomass_kgha",
  predictors = NULL,
  method = c("lm", "rf", "auto"),
  group = "pasture",
  num_trees = 500L
)
}
\arguments{
\item{data}{Calibration data frame from \code{\link[=build_biomass_calibration]{build_biomass_calibration()}}.}

\item{response}{Biomass column (kg/ha).}

\item{predictors}{Optional predictor override applied to both routes. When
\code{NULL}, each route uses its paper's default set present in \code{data} (the
parsimonious Page set for the LM, the richer Vahidi CHM-statistics + index
set for the RF).}

\item{method}{\code{"lm"}, \code{"rf"}, or \code{"auto"} (compare both).}

\item{group}{Optional categorical column (e.g. \code{pasture}) added to the RF.}

\item{num_trees}{Random-forest tree count when \code{ranger} is used.}
}
\value{
An object of class \code{dronebio_biomass_model} with the chosen \code{fit},
its \code{method}, the predictor names, LOO-CV \code{metrics}, and (for \code{auto}) the
per-method comparison.
}
\description{
Stage 1 fits the defensible pooled linear model (Page et al. 2025:
vegetation volume / canopy height + greenness). Stage 2 optionally fits a
random forest (Vahidi et al. 2023: CHM statistics + spectral values + a
categorical pasture label) when the \code{ranger} package is available. With
\code{method = "auto"} both are fitted and the one with the lower leave-one-out
RMSE is returned as the primary model.
}
\examples{
set.seed(1)
v <- runif(30, 0, 0.5); g <- runif(30, 0.3, 0.9)
d <- data.frame(biomass_kgha = 500 + 4000 * v + 1500 * g + rnorm(30, sd = 120),
                chm_volume_m3 = v, NDVI = g)
m <- fit_biomass_model(d, predictors = c("chm_volume_m3", "NDVI"))
m$metrics$r2
}
