% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/products.R
\name{quick_outputs_check}
\alias{quick_outputs_check}
\title{Lightweight existence + size check on ODM outputs}
\usage{
quick_outputs_check(project, min_size_mb = 1)
}
\arguments{
\item{project}{A \code{dronebio_project} object.}

\item{min_size_mb}{Numeric size threshold (MB) below which a file
is treated as a placeholder / aborted artifact. Default 1 MB.}
}
\value{
Named logical vector with \code{orthomosaic}, \code{dsm}, \code{dtm},
\code{point_cloud}, plus a top-level \code{outputs_complete} summary that
is TRUE iff the orthomosaic and DSM both pass.
}
\description{
Cheap counterpart to \code{\link[=validate_odm_outputs]{validate_odm_outputs()}} that never opens a
raster file (so it doesn't trigger OneDrive Files-On-Demand
downloads on cloud-synced project folders). Just checks that the
orthomosaic and DSM exist and are bigger than \code{min_size_mb} MB.
}
\examples{
p <- dronebio_project(project_dir = tempdir())
quick_outputs_check(p)
}
