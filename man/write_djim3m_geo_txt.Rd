% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dji_ppk.R
\name{write_djim3m_geo_txt}
\alias{write_djim3m_geo_txt}
\title{Write an ODM \code{geo.txt} from a .MRK folder + a list of filenames}
\usage{
write_djim3m_geo_txt(
  images_dir,
  image_filenames,
  geo_txt_path,
  min_fix_quality = 4L
)
}
\arguments{
\item{images_dir}{Folder holding the .MRK file(s).}

\item{image_filenames}{Character vector of image \emph{basenames} (not
full paths). Each is looked up by its photo-number suffix.}

\item{geo_txt_path}{Destination path for the ODM geo.txt.}

\item{min_fix_quality}{Drop rows whose fix quality is below this
threshold. Default 4 (RTK Float) — anything below would not
constrain the bundle adjustment usefully. Set to 0 to keep
everything.}
}
\value{
Invisibly a list with \code{written} (the path), \code{matched},
\code{unmatched} (filenames that had no .MRK row), \code{dropped_quality}
(filenames whose row was below \code{min_fix_quality}).
}
\description{
For each entry in \code{image_filenames}, resolves the photo number
from the filename, looks up the matching row in the merged
.MRK data, and emits one geo.txt row:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{<filename> <lon> <lat> <alt> <yaw> <pitch> <roll> <horiz_acc> <vert_acc>
}\if{html}{\out{</div>}}

Yaw/pitch/roll are emitted as \code{0} because the .MRK does not
record them — ODM treats those columns as optional anyway.
Accuracy columns come straight from the .MRK std deviations
(horiz_acc = sqrt(lat_std^2 + lon_std^2)).
}
\examples{
\dontrun{
  files <- list_aerial_images("/path/to/ifasbahia10")
  write_djim3m_geo_txt(
    images_dir      = "/path/to/ifasbahia10",
    image_filenames = files$filename,
    geo_txt_path    = "/tmp/geo.txt"
  )
}
}
