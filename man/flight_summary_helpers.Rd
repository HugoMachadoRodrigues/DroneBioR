% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time_series.R
\name{flight_summary_helpers}
\alias{flight_summary_helpers}
\alias{flight_ndvi_mean}
\alias{flight_biomass_proxy_mean}
\alias{flight_chm_mean}
\title{Stock summary helpers for \code{\link[=flight_time_series]{flight_time_series()}}}
\usage{
flight_ndvi_mean(project)

flight_biomass_proxy_mean(project)

flight_chm_mean(project)
}
\arguments{
\item{project}{A \code{dronebio_project} object.}
}
\value{
A single numeric value, or \code{NA} when the underlying product
is missing.
}
\description{
Each helper accepts a \code{dronebio_project} and returns a single numeric
value, suitable for use as the \code{summary_fn} argument of
\code{\link[=flight_time_series]{flight_time_series()}}.
}
\examples{
\dontrun{
# Each helper reads the products of one flight, so point dronebio_project()
# at a directory that already holds ODM output.
project <- dronebio_project("~/flights/2026-05-01")
flight_ndvi_mean(project)
flight_biomass_proxy_mean(project)
flight_chm_mean(project)
}
}
