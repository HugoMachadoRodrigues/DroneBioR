% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pointcloud.R
\name{despike_ply}
\alias{despike_ply}
\title{Remove reconstruction spikes from a point-cloud file}
\usage{
despike_ply(path, out_path = path, backup = TRUE, backup_path = NULL, ...)
}
\arguments{
\item{path}{Source binary PLY.}

\item{out_path}{Destination; defaults to \code{path} (in place).}

\item{backup, backup_path}{Passed to \code{\link[=write_ply_subset]{write_ply_subset()}}; by default the
untouched original is kept next to the cloud.}

\item{...}{Despike tuning passed to \code{\link[=despike_point_cloud]{despike_point_cloud()}} (\code{methods},
\code{sor_k}, \code{sor_mult}, \code{cell}, \code{ground_q}, \code{smooth_w}, \code{height_cap},
\code{surface_mult}).}
}
\value{
Invisibly, a list with \code{n_before}, \code{n_after}, \code{n_removed}.
}
\description{
Reads a binary PLY in full, flags spikes with \code{\link[=despike_point_cloud]{despike_point_cloud()}}, and
writes the cleaned cloud back with \code{\link[=write_ply_subset]{write_ply_subset()}} -- preserving every
vertex property (colours, normals) so the result still meshes. The default
rewrites the file in place after snapshotting the untouched original, so the
app's "restore" and rerun-from-\code{odm_meshing} flow work unchanged.
}
\examples{
\dontrun{
ply <- file.path(project$odm_project_dir, "odm_filterpoints", "point_cloud.ply")
despike_ply(ply, backup_path = sub("\\\\.ply$", ".original.ply", ply))
}
}
