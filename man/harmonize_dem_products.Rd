% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/products.R
\name{harmonize_dem_products}
\alias{harmonize_dem_products}
\title{Produce physically consistent DSM, DTM and CHM}
\usage{
harmonize_dem_products(
  project = NULL,
  dsm = NULL,
  dtm = NULL,
  out_dir = NULL,
  canopy_ceiling = 30,
  trend_cell_m = 30,
  max_depth_below_ground = 2,
  iterations = 2L,
  dtm_max_bump = 5,
  write = TRUE
)
}
\arguments{
\item{project}{Optional \code{dronebio_project}; when supplied the DSM
and DTM are taken from \code{\link[=odm_product_paths]{odm_product_paths()}} and outputs default
to the same \verb{odm_dem/} folder.}

\item{dsm, dtm}{Raster paths or \code{terra::SpatRaster}s. Required when
\code{project} is not given.}

\item{out_dir}{Output directory. Defaults to the DSM's folder.
The function writes \code{dsm_consistent.tif}, \code{dtm_consistent.tif}
and \code{chm_consistent.tif} there when \code{write = TRUE}.}

\item{canopy_ceiling}{Height (m) above the local canopy trend
beyond which a CHM cell is treated as a tower spike and removed.
Default \code{30} — keeps genuine tall trees, drops the reconstruction
towers.}

\item{trend_cell_m, max_depth_below_ground, iterations}{Passed to
\code{\link[=despike_dem]{despike_dem()}} for the DTM and CHM cleaning. Defaults \code{30}, \code{2},
\code{2}.}

\item{dtm_max_bump}{Height (m) above its own trend beyond which a
DTM cell is treated as a spike. Default \code{5}.}

\item{write}{Logical. Write the three GeoTIFFs. Default \code{TRUE}.}
}
\value{
Invisibly, a list with the cleaned \code{dsm}, \code{dtm}, \code{chm}
\code{terra::SpatRaster}s and, when written, their \code{paths}.
}
\description{
ODM generates the DSM (top of the dense cloud) and the DTM
(SMRF-classified ground, interpolated) by independent processes, so
they are not pixel-consistent: on bare ground the interpolated DTM
routinely sits a few centimetres \emph{above} the DSM, which makes
\code{DSM - DTM} (the canopy height) negative over a large fraction of a
short-canopy survey. Despiking the two rasters separately can widen
that gap further. This helper rebuilds all three products so they
obey the physical constraints \code{CHM >= 0} and \code{DSM >= DTM}
everywhere, by construction:
}
\details{
\enumerate{
\item Despike the DTM (the ground) with \code{\link[=despike_dem]{despike_dem()}}.
\item Form \code{CHM = DSM - DTM_clean}, clamp it to \verb{>= 0} (which
turns the DSM's downward pits — where the surface dipped below
ground — back into bare ground) and despike the result to
remove canopy-height towers / needles.
\item Rebuild \code{DSM = DTM_clean + CHM_clean}. Because the cleaned
CHM is non-negative, the rebuilt DSM is never below the DTM.
}
}
\examples{
\dontrun{
  project <- dronebio_project("~/flight")
  harmonize_dem_products(project)            # writes *_consistent.tif
}
}
