% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field_extract.R
\name{covariate_frame_from_pixels}
\alias{covariate_frame_from_pixels}
\title{Compute a covariate frame from pixel values}
\usage{
covariate_frame_from_pixels(
  band_values,
  selected,
  chm_values = NULL,
  custom_values = NULL,
  dsm_values = NULL,
  dtm_values = NULL,
  crs = ""
)
}
\arguments{
\item{band_values}{Numeric matrix / data frame of reflectance values, one
column per band (\code{Blue}, \code{Green}, \code{Red}, \code{RedEdge}, \code{NIR}, ...).}

\item{selected}{Character vector of covariate ids to return, in order.}

\item{chm_values}{Optional canopy-height values (one per pixel).}

\item{custom_values}{Optional custom-index values: a named vector, or a
data frame whose column names are the covariate ids.}

\item{dsm_values, dtm_values}{Optional terrain values (one per pixel).}

\item{crs}{CRS for the synthetic grid; must be the same for all stacks
compared inside one call.}
}
\value{
A data frame with \code{nrow(band_values)} rows and columns named
exactly \code{selected}, in that order.
}
\description{
The single definition of "how a covariate is computed from a pixel".
Extraction, map prediction and the full-resolution export all route
through this function, so training and prediction can never diverge.
}
\details{
Indices are computed by packing the pixels into a one-row raster
(\code{\link[=synthesize_pixel_raster]{synthesize_pixel_raster()}}) and calling \code{\link[=compute_spectral_indices]{compute_spectral_indices()}}.
Biomass proxies get the CHM as an extra layer on that \strong{same} one-row
grid, so \code{terra::compareGeom()} inside \code{\link[=compute_biomass_proxies]{compute_biomass_proxies()}}
succeeds and its no-resample branch is taken - resampling a one-row
raster would be meaningless.
}
\examples{
bands <- data.frame(Green = c(0.2, 0.3), Red = c(0.1, 0.15),
                    RedEdge = c(0.3, 0.35), NIR = c(0.6, 0.7))
covariate_frame_from_pixels(bands, c("NDVI", "NDRE", "Red"))
}
