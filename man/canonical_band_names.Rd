% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster.R
\name{canonical_band_names}
\alias{canonical_band_names}
\title{Canonical band names from whatever a raster calls its layers}
\usage{
canonical_band_names(x)
}
\arguments{
\item{x}{Character vector of layer names, or a \code{SpatRaster}.}
}
\value{
A character vector the same length as \code{x}, holding canonical names
or \code{NA}.
}
\description{
Maps layer names to the canonical \code{Blue} / \code{Green} / \code{Red} / \code{RedEdge} /
\code{NIR} used throughout the package, so band detection does not depend on one
vendor's spelling. Recognises the common forms seen in the wild:
MicaSense (\code{Red}, \code{Rededge}, \code{NIR}), the DJI Mavic 3M stack (\code{MS_R},
\code{MS_RE}, \code{MS_NIR}), Parrot Sequoia (\code{red}, \code{red_edge}, \code{nir}), plain
one-letter names, and \verb{band_*} / \verb{b*} prefixes.
}
\details{
Order matters and the rules are deliberately specific-first: \code{RE} and
\code{rededge} must win over \code{red}, and \code{NIR} over \code{N}, or a red-edge layer gets
silently filed as red and every index built on it is quietly wrong.

Anything unrecognised returns \code{NA}, which callers should treat as "this band
is absent" rather than guessing.
}
\examples{
canonical_band_names(c("Red", "Green", "Blue", "MS_RE", "MS_NIR"))
canonical_band_names(c("b1", "b2", "b3"))
}
