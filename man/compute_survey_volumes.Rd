% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survey_volumes.R
\name{compute_survey_volumes}
\alias{compute_survey_volumes}
\title{Survey-grade volume calculations over a region of interest}
\usage{
compute_survey_volumes(
  top,
  roi,
  method = c("dtm", "min_z", "mean_z", "ground_quantile", "user_plane", "perimeter_tin"),
  dtm = NULL,
  base_z = NULL,
  ground_quantile = 0.05
)
}
\arguments{
\item{top}{A \code{terra::SpatRaster} containing the top surface (DSM,
CHM, etc.). Must be in a projected CRS with linear units;
otherwise volumes are not meaningful.}

\item{roi}{A data frame with \code{x} and \code{y} columns describing the ROI
polygon in the same CRS as \code{top}. At least three vertices. The
ring is closed automatically.}

\item{method}{Base-reference method. One of \code{"dtm"}, \code{"min_z"},
\code{"mean_z"}, \code{"ground_quantile"}, \code{"user_plane"}, \code{"perimeter_tin"}.}

\item{dtm}{Required when \code{method = "dtm"}. A \code{terra::SpatRaster}
bare-earth DTM. Resampled to the \code{top} grid if geometries differ.}

\item{base_z}{Required when \code{method = "user_plane"}. Numeric.}

\item{ground_quantile}{Quantile in \verb{[0, 1]} used by
\code{method = "ground_quantile"}. Default \code{0.05}.}
}
\value{
A list with class \code{"dronebio_survey_volume"}:

\describe{
\item{\code{method}}{Method used.}
\item{\code{cut_volume_m3}}{Volume above the base (m^3).}
\item{\code{fill_volume_m3}}{Volume below the base (m^3, positive).}
\item{\code{net_volume_m3}}{\code{cut - fill} (m^3).}
\item{\code{surface_area_planar_m2}}{Projected (planimetric) area of
valid cells inside the ROI.}
\item{\code{surface_area_draped_m2}}{3D surface area via the cell-wise
slope tangent; \code{NA} if \code{terra::terrain()} cannot be computed
(e.g. ROI < 3x3 cells).}
\item{\code{perimeter_m}}{Planimetric polygon perimeter.}
\item{\code{cell_count}}{Number of valid \code{top}-cells inside the ROI.}
\item{\code{cell_area_m2}}{Grid cell area, m^2.}
\item{\code{top_z_summary}}{Named vector \code{min/median/mean/max} of the
top surface inside the ROI.}
\item{\code{base_z_summary}}{Same for the base surface.}
\item{\code{height_summary}}{Same for \code{(top - base)}.}
\item{\code{base_reference_text}}{Human-readable description of the
base method.}
}
}
\description{
Computes the volume between a "top" surface (a DSM, a CHM, or any other
height raster) and a chosen base reference, broken into cut (height
above the base), fill (height below the base) and net volumes. The
base reference is chosen with \code{method}, and matches the conventions
used by photogrammetric surveyors for stockpiles, earthworks and
biomass calculations.
}
\details{
The volume math is the standard raster integral
\deqn{V = \sum_i (z_i^{top} - z_i^{base}) \cdot A_{cell}}
clipped to positive (cut) and negative (fill) contributions, where
the sum runs over every cell of \code{top} whose centre lies inside the
ROI polygon. The base surface is built on the same grid as the
clipped \code{top}, so cut and fill are computed cell-by-cell with no
resampling artefact between top and base.

Base-reference methods:

\describe{
\item{\code{"dtm"}}{Use a separate DTM (bare-earth model) as the base.
This is the correct method for canopy biomass when a DTM is
available: the cut volume reduces to \eqn{\int (DSM - DTM) dA},
i.e. the canopy height model integrated over the ROI.}
\item{\code{"min_z"}}{Constant plane at the minimum top-surface value
inside the ROI. The classic stockpile method when no separate
bare-earth model exists.}
\item{\code{"mean_z"}}{Constant plane at the mean top-surface value.}
\item{\code{"ground_quantile"}}{Constant plane at a low quantile of the
top surface (default 5th percentile). A robust proxy for the
ground when there is no DTM and \code{"min_z"} is too sensitive to a
single dark pixel.}
\item{\code{"user_plane"}}{Constant plane at the user-supplied \code{base_z}.}
\item{\code{"perimeter_tin"}}{Triangulated irregular network built from
the top-surface values sampled at the ROI's perimeter vertices.
This is the industry standard for stockpile surveys (used by
Pix4D Mapper, Bentley ContextCapture, Trimble Business Center):
the perimeter rests on the surveyed "natural ground line", a
Delaunay triangulation interpolates linearly between those
vertices, and the volume is the integral of \code{top - TIN} inside
the ROI. Requires the optional \code{interp} package.}
}

Both planimetric and 3D draped surface areas are reported. The
draped area is the sum of cell areas inflated by the secant of the
local slope, \eqn{A_{drape} = \sum_i A_{cell} \cdot \sec(\theta_i)},
which is the standard formula in
photogrammetric reporting.
}
\examples{
dsm <- terra::rast(system.file("extdata", "dsm_subset.tif", package = "DroneBioR"))
dtm <- terra::rast(system.file("extdata", "dtm_subset.tif", package = "DroneBioR"))
roi <- data.frame(
  x = c(392004, 392012, 392012, 392004),
  y = c(3033004, 3033004, 3033012, 3033012)
)
result <- compute_survey_volumes(top = dsm, roi = roi, method = "dtm", dtm = dtm)
result$cut_volume_m3

# Constant-plane stockpile-style:
result_min <- compute_survey_volumes(top = dsm, roi = roi, method = "min_z")
result_min$cut_volume_m3
}
