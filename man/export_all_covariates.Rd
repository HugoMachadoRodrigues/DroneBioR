% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indices.R
\name{export_all_covariates}
\alias{export_all_covariates}
\title{Compute and export every covariate to a folder}
\usage{
export_all_covariates(
  reflectance,
  out_dir,
  chm = NULL,
  dsm = NULL,
  dtm = NULL,
  custom_index = NULL,
  overwrite = TRUE
)
}
\arguments{
\item{reflectance}{A \code{terra::SpatRaster} of the scaled reflectance bands
(named, e.g. \code{Red}, \code{Green}, \code{NIR}, \code{RedEdge}).}

\item{out_dir}{Destination folder. Created if missing.}

\item{chm, dsm, dtm}{Optional terrain \code{SpatRaster}s; each exported when given,
and the CHM also feeds the \verb{*_x_CHM} biomass proxies.}

\item{custom_index}{Optional single-layer \code{SpatRaster} of a user index.}

\item{overwrite}{Overwrite existing files (default \code{TRUE}); when \code{FALSE},
existing outputs are kept and still reported as written.}
}
\value{
Invisibly, a character vector of the GeoTIFFs written.
}
\description{
Builds the full covariate set the Field Models tab can use -- the reflectance
bands, every spectral index \code{\link[=compute_spectral_indices]{compute_spectral_indices()}} can derive, the
biomass proxies \code{\link[=compute_biomass_proxies]{compute_biomass_proxies()}} (the canopy-height ones when a
CHM is supplied), and the terrain layers -- and writes each as its own
GeoTIFF into \code{out_dir} (a folder named \code{covariates/} by convention), at the
orthomosaic's native resolution, ready to load into any GIS afterwards.
}
\details{
Layers that cannot be produced (e.g. the biomass proxies when the cloud has
none of the bands they need) are skipped, not fatal, so the export always
returns whatever could be built.
}
\examples{
\dontrun{
refl <- read_multispectral_orthomosaic("odm_orthophoto.tif")
chm  <- build_chm_from_dsm_dtm("odm_dem/dsm.tif", "odm_dem/dtm.tif")
export_all_covariates(refl, file.path(project$project_dir, "covariates"),
                      chm = chm)
}
}
