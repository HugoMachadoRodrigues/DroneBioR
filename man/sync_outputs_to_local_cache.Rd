% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/products.R
\name{sync_outputs_to_local_cache}
\alias{sync_outputs_to_local_cache}
\title{Copy ODM outputs to a fast local cache once, return the new paths}
\usage{
sync_outputs_to_local_cache(
  project,
  cache_root = file.path(Sys.getenv("HOME"), ".dronebior", "cache"),
  products = c("orthomosaic", "dsm", "dtm", "point_cloud_copc", "point_cloud_laz",
    "textured_obj", "textured_glb")
)
}
\arguments{
\item{project}{A \code{dronebio_project} object pointing at the
(typically cloud-synced) outputs you want to migrate.}

\item{cache_root}{Root for the cache. Defaults to
\verb{~/.dronebior/cache}.}

\item{products}{Subset of product keys (see \code{\link[=odm_product_paths]{odm_product_paths()}})
to copy. Default covers the analysis essentials.}
}
\value{
List with \code{cache_dir} and a named \code{paths} character vector
keyed by product (only entries actually present on disk).
}
\description{
Use when the project root lives inside OneDrive / Google Drive /
Dropbox and the heavy raster + point-cloud files keep triggering
background re-syncs. After this returns, the Shiny app can be
repointed at the local cache and never touch the cloud-synced
folder again. Files are skipped if a same-size copy already exists.
}
\examples{
\dontrun{
  project <- dronebio_project("~/cloud_project")
  cache <- sync_outputs_to_local_cache(project)
  cache$paths[["orthomosaic"]]
}
}
