% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine_odm.R
\name{refilter_odm_point_cloud}
\alias{refilter_odm_point_cloud}
\title{Re-clean an ODM point cloud without redoing the reconstruction}
\usage{
refilter_odm_point_cloud(
  project,
  pc_filter = 2.5,
  pc_sample = NULL,
  pc_rectify = FALSE,
  ...
)
}
\arguments{
\item{project}{A \code{dronebio_project} whose ODM project directory already
holds a finished (or at least past-\code{openmvs}) run.}

\item{pc_filter, pc_sample, pc_rectify}{Point-cloud cleanup settings, as in
\code{\link[=build_odm_args]{build_odm_args()}}.}

\item{...}{Further arguments passed to \code{\link[=run_odm_project]{run_odm_project()}}, e.g.
\code{build_dsm}, \code{build_dtm}, \code{camera_type}.}
}
\value{
Invisibly, the list returned by \code{\link[=run_odm_project]{run_odm_project()}}.
}
\description{
Reruns an existing ODM project from the \code{odm_filterpoints} stage with new
point-cloud filter settings. Everything before that stage -- \code{opensfm} and
\code{openmvs}, which together are the bulk of the runtime and disk of a run --
is reused from the project directory, while meshing, texturing, the DEMs and
the orthophoto are rebuilt from the newly filtered cloud.
}
\details{
This is the loop to use when tuning \code{pc_filter} against the 3D view: the
reconstruction is paid for once, and each retune costs only the downstream
stages.
}
\examples{
\dontrun{
project <- dronebio_project("~/flights/2026-05-01")
# First pass at ODM's default-ish setting, then tighten twice; only the
# stages after odm_filterpoints are recomputed each time.
refilter_odm_point_cloud(project, pc_filter = 2.5, build_dsm = TRUE)
refilter_odm_point_cloud(project, pc_filter = 1.5, pc_rectify = TRUE,
                         build_dsm = TRUE, build_dtm = TRUE)
}
}
