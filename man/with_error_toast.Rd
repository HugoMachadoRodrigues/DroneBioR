% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_helpers.R
\name{with_error_toast}
\alias{with_error_toast}
\title{Run an expression and report errors as Shiny toasts}
\usage{
with_error_toast(label, expr, session = NULL)
}
\arguments{
\item{label}{Short label for the operation; appears at the start of the
toast.}

\item{expr}{Expression to evaluate. Lazy.}

\item{session}{Shiny session. Defaults to the current reactive domain.}
}
\value{
The value of \code{expr}, or \code{NULL} on error.
}
\description{
Wraps a reactive body (or any side-effecting expression in a Shiny
session) so that uncaught errors surface as toast notifications instead
of the default red traceback. Useful inside \code{reactive()},
\code{eventReactive()}, \code{observeEvent()} and render functions.
}
\details{
Shiny's own \code{validate()} / \code{req()} / \code{need()} raise the
\code{shiny.silent.error} condition, which \code{with_error_toast()} re-throws so
those inline validations keep working unchanged.

Outside a Shiny session the error is re-emitted via \code{warning()} so the
helper is safe to call from package-level code too.
}
\examples{
\dontrun{
mosaic <- eventReactive(input$load_mosaic, {
  with_error_toast("Load orthomosaic", {
    read_multispectral_orthomosaic(input$orthomosaic_path)
  })
})
}

# Outside Shiny, errors fall back to warning():
suppressWarnings(
  result <- with_error_toast("Demo", stop("nope"), session = NULL)
)
is.null(result)
}
