% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time_series.R
\name{register_flight}
\alias{register_flight}
\title{Register a flight in the time-series registry}
\usage{
register_flight(
  date,
  project_dir,
  notes = "",
  registry_path = default_flight_registry(),
  odm_dataset_subdir = NA_character_,
  odm_project_name = NA_character_
)
}
\arguments{
\item{date}{Date or character that \code{as.Date()} can parse.}

\item{project_dir}{Project directory for the flight. Will be
\code{normalizePath}-ed.}

\item{notes}{Optional free-text notes.}

\item{registry_path}{Path to the registry CSV. Defaults to
\code{\link[=default_flight_registry]{default_flight_registry()}}.}

\item{odm_dataset_subdir, odm_project_name}{Optional ODM sub-project
coordinates for this flight (as passed to \code{\link[=dronebio_project]{dronebio_project()}}).
Recorded so the flight can later be rebuilt with the exact ODM run that
produced its products, rather than the \code{micasense} default. When left
\code{NA}, \code{\link[=flight_time_series]{flight_time_series()}} falls back to
the \code{dronebio_project()} defaults.}
}
\value{
Invisibly returns the updated registry data frame.
}
\description{
Appends one row to the registry CSV. The \code{flight_id} is auto-generated
from the date plus a short hash of the project directory, so repeated
calls with the same date and project_dir are idempotent.
}
\examples{
# Registering a flight only records a path, so a throwaway directory is
# enough to demonstrate it; in practice project_dir is the ODM project
# directory produced by the flight.
reg <- tempfile(fileext = ".csv")
register_flight(date = Sys.Date(), project_dir = tempdir(),
                registry_path = reg)
list_flights(reg)
}
