% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field_caret.R
\name{fit_field_caret_model}
\alias{fit_field_caret_model}
\title{Fit one caret model on a shared train / test split}
\usage{
fit_field_caret_model(
  data,
  response = "biomass_kgha",
  predictors,
  method = "lm",
  metric = c("RMSE", "Rsquared"),
  split,
  preprocess = NULL,
  allow_parallel = FALSE
)
}
\arguments{
\item{data}{Extraction table from \code{\link[=extract_field_covariates]{extract_field_covariates()}}.}

\item{response}{Response column.}

\item{predictors}{Covariate ids, in order.}

\item{method}{caret method id.}

\item{metric}{\code{"RMSE"} (minimised) or \code{"Rsquared"} (maximised). This is
caret's internal ranking metric only.}

\item{split}{A \code{\link[=field_train_split]{field_train_split()}} result, shared across the sweep.}

\item{preprocess}{Optional \code{caret::preProcess} steps, e.g.
\code{c("center", "scale")}.}

\item{allow_parallel}{Passed to \code{trainControl()}. Keep \code{FALSE} inside
Shiny, which already runs a \code{future} plan.}
}
\value{
An object of class \code{dronebio_field_model}.
}
\description{
Runs 10-fold cross-validation inside the training partition to select
hyperparameters, then reports three honest metric rows: out-of-fold CV,
resubstitution on the training rows, and the independent holdout.
}
\examples{
if (requireNamespace("caret", quietly = TRUE)) {
  set.seed(1)
  d <- data.frame(NDVI = runif(60, 0.2, 0.9))
  d$biomass_kgha <- 800 + 3000 * d$NDVI + rnorm(60, sd = 150)
  split <- field_train_split(d, holdout = 0.25, folds = 5)
  m <- fit_field_caret_model(d, predictors = "NDVI", method = "lm",
                             split = split)
  m$metrics
}
}
