% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field_ingest.R
\name{stage_uploaded_vector}
\alias{stage_uploaded_vector}
\title{Stage a Shiny multi-file upload back onto disk}
\usage{
stage_uploaded_vector(name, datapath, dir = tempfile("dronebio_field_"))
}
\arguments{
\item{name}{Character vector of original file names (\code{input$file$name}).}

\item{datapath}{Character vector of temporary paths
(\code{input$file$datapath}), same length as \code{name}.}

\item{dir}{Directory to stage into. Created if missing.}
}
\value{
The path of the dataset to open, with attributes \code{staged_dir}
(the directory holding every part) and \code{crs_known} (\code{FALSE} when a
shapefile arrived without a \code{.prj}).
}
\description{
Shiny writes uploaded files to its own temporary names (\verb{0.dbf}, \verb{1.prj},
\verb{2.shp}, \verb{3.shx}), and GDAL cannot open a shapefile whose sidecars no
longer share the base name - \code{sf::st_read()} fails with
\verb{Unable to open .../2.shx}. This helper copies each upload back under its
original \code{name} so the parts line up again, and unpacks a single \code{.zip}
upload the same way.
}
\examples{
tmp <- tempfile(fileext = ".csv")
utils::write.csv(data.frame(sample_id = "S01", biomass_kgha = 1000,
                            x = 1, y = 2), tmp, row.names = FALSE)
staged <- stage_uploaded_vector("field.csv", tmp)
basename(staged)
}
