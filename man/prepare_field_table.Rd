% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field_ingest.R
\name{prepare_field_table}
\alias{prepare_field_table}
\title{Rename, convert and reproject field points for modelling}
\usage{
prepare_field_table(
  points,
  id_col,
  biomass_col,
  units = c("kg/ha", "Mg/ha", "g/m^2", "unknown"),
  target_crs
)
}
\arguments{
\item{points}{An \code{sf} POINT layer from \code{\link[=read_field_points]{read_field_points()}}.}

\item{id_col}{Column holding the sample identifier.}

\item{biomass_col}{Column holding the biomass measurement.}

\item{units}{Units of \code{biomass_col}: \code{"kg/ha"}, \code{"Mg/ha"}, \code{"g/m^2"} or
\code{"unknown"} (passed through unchanged).}

\item{target_crs}{CRS of the raster stack the samples will be extracted
against.}
}
\value{
An \code{sf} POINT layer of class \code{dronebio_field_points}, carrying a
\code{dropped_na} attribute with the number of rows removed for a missing
biomass value.
}
\description{
Produces the canonical field table every downstream function expects:
\code{sample_id} (character) and \code{biomass_kgha} (numeric), in the raster CRS.
All other attributes are preserved untouched.
}
\examples{
path <- system.file("extdata", "field_samples.csv", package = "DroneBioR")
pts <- read_field_points(path, crs = 32617)
tab <- prepare_field_table(pts, "sample_id", "biomass_kgha",
                           units = "kg/ha", target_crs = "EPSG:32617")
names(tab)[1:2]
}
