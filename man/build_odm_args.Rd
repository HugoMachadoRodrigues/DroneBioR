% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine_odm.R
\name{build_odm_args}
\alias{build_odm_args}
\title{Build an ODM Docker command}
\usage{
build_odm_args(
  dataset_dir,
  project_name = "micasense",
  image = "opendronemap/odm",
  camera_type = c("multispectral", "rgb"),
  radiometric_calibration = NULL,
  orthophoto_resolution_cm = 5,
  max_concurrency = 4,
  fast_orthophoto = TRUE,
  build_dsm = FALSE,
  build_dtm = FALSE,
  pc_quality = NULL,
  pc_filter = 2.5,
  pc_sample = NULL,
  pc_rectify = FALSE,
  pc_las = FALSE,
  pc_csv = FALSE,
  pc_copc = FALSE,
  tiles = FALSE,
  three_d_tiles = FALSE,
  gltf = FALSE,
  skip_3dmodel = FALSE,
  skip_report = FALSE,
  rerun_from = NULL,
  end_with = NULL,
  extra_args = character()
)
}
\arguments{
\item{dataset_dir}{Host folder mounted to \verb{/datasets}.}

\item{project_name}{ODM project name inside \code{dataset_dir}.}

\item{image}{Docker image name.}

\item{camera_type}{One of \code{"multispectral"} (MicaSense / Sequoia-style 5-band
cameras, applies \verb{--radiometric-calibration camera+sun} by default) or
\code{"rgb"} (Sony, DJI, Phantom, generic RGB - skips the radiometric flag
because it does not apply).}

\item{radiometric_calibration}{ODM radiometric-calibration value. When
\code{NULL}, the default is chosen by \code{camera_type}: \code{"camera+sun"} for
multispectral, omitted for RGB. Set to \code{"none"} to skip explicitly.}

\item{orthophoto_resolution_cm}{Orthophoto resolution in centimeters.}

\item{max_concurrency}{Maximum concurrent ODM workers.}

\item{fast_orthophoto}{Logical. Add \code{--fast-orthophoto}.}

\item{build_dsm}{Logical. Add DSM generation.}

\item{build_dtm}{Logical. Add DTM generation options.}

\item{pc_quality}{Densification detail (\code{--pc-quality}): one of \code{"ultra"},
\code{"high"}, \code{"medium"}, \code{"low"}, \code{"lowest"}. Each step up produces a denser
cloud and costs roughly four times the time. \code{NULL} (default) leaves ODM's
own default of \code{"medium"}.}

\item{pc_filter}{Statistical outlier removal, in standard deviations from
the local mean (\code{--pc-filter}). Applied in ODM's \code{odm_filterpoints}
stage, which runs \emph{before} meshing, texturing, the DEMs and the
orthophoto, so it is the one knob that cleans reconstruction noise out of
every downstream product at once. ODM's own default is 5, which is loose
enough to leave floating specks and needles in the point cloud; the
default here is 2.5. Use \code{0} to disable filtering.}

\item{pc_sample}{Optional thinning radius in metres (\code{--pc-sample}): keeps a
single point per sphere of this radius. \code{NULL} (default) keeps every
point.}

\item{pc_rectify}{Logical. Re-classify wrongly classified ground points and
fill gaps (\code{--pc-rectify}). Worth enabling when the DTM matters.}

\item{pc_las}{Logical. Export LAS point cloud.}

\item{pc_csv}{Logical. Export CSV point cloud.}

\item{pc_copc}{Logical. Export COPC point cloud.}

\item{tiles}{Logical. Export web map tiles.}

\item{three_d_tiles}{Logical. Export 3D tiles.}

\item{gltf}{Logical. Export glTF model.}

\item{skip_3dmodel}{Logical. Add \verb{--skip-3dmodel} to skip the
ODM \code{odm_meshing} and \code{mvs_texturing} stages. Saves 10-30 min
on a typical 300-image flight when the consumer only needs
DSM / DTM / orthomosaic. The skipped artifacts are the textured
3D \code{.obj} / \code{.glb} files.}

\item{skip_report}{Logical. Add \code{--skip-report} to skip ODM's
PDF report generation stage. Saves ~1-2 min and avoids the
known \code{gdal_translate} / numpy ABI crash inside some
\code{opendronemap/odm} Docker images.}

\item{rerun_from}{Optional ODM stage.}

\item{end_with}{Optional ODM end stage.}

\item{extra_args}{Additional ODM arguments.}
}
\value{
Character vector of arguments for \code{system2("docker", args)}.
}
\description{
Build an ODM Docker command
}
\examples{
args <- build_odm_args(
  dataset_dir = tempdir(),
  project_name = "demo",
  build_dsm = TRUE,
  build_dtm = TRUE
)
head(args)

# RGB camera (Sony / DJI / Phantom): no radiometric calibration flag.
rgb_args <- build_odm_args(
  dataset_dir  = tempdir(),
  project_name = "rgb_flight",
  camera_type  = "rgb",
  build_dsm    = TRUE
)
"--radiometric-calibration" \%in\% rgb_args
}
