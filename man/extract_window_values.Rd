% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field_extract.R
\name{extract_window_values}
\alias{extract_window_values}
\title{Aggregate raster values over pre-computed window cells}
\usage{
extract_window_values(
  x,
  cells,
  fun = c("mean", "median", "max", "min", "sd"),
  na.rm = TRUE
)
}
\arguments{
\item{x}{A \code{terra::SpatRaster}.}

\item{cells}{Integer matrix of cell numbers, as returned in the \code{cells}
element of \code{\link[=window_cells]{window_cells()}}.}

\item{fun}{Aggregation: \code{"mean"}, \code{"median"}, \code{"max"}, \code{"min"} or \code{"sd"}.}

\item{na.rm}{Drop missing window pixels before aggregating.}
}
\value{
A data frame with \code{nrow(cells)} rows, one numeric column per layer
of \code{x} (named after the layer), plus \code{.n_valid_px} - the number of window
pixels with data in the first layer.
}
\description{
One windowed \code{terra::extract()} read per call, reshaped per layer and
collapsed with \code{fun}. Cells that fall outside the raster come back as
\code{NA}, so an edge point aggregates over its in-raster pixels only.
}
\details{
Never use \code{terra::extract(..., buffer =)} for this: terra accepts and
silently ignores \code{buffer} for point extraction (unlike \code{raster::extract}),
returning single-cell values that look plausible.
}
\examples{
ortho <- system.file("extdata", "micasense_subset.tif", package = "DroneBioR")
r <- terra::rast(ortho)[[1:2]]
pts <- data.frame(x = 392004, y = 3033007)
wc <- window_cells(r, pts, window = 3)
extract_window_values(r, wc$cells, fun = "mean")
}
