% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine_webodm.R
\name{run_webodm_project}
\alias{run_webodm_project}
\title{Run a DroneBioR project through a remote WebODM instance}
\usage{
run_webodm_project(
  project,
  base_url,
  username,
  password,
  project_name = NULL,
  camera_type = c("multispectral", "rgb"),
  poll_seconds = 60,
  ...
)
}
\arguments{
\item{project}{A \code{dronebio_project} object.}

\item{base_url, username, password}{WebODM server URL and credentials.}

\item{project_name}{WebODM project name (default: from \code{project}).}

\item{camera_type}{\code{"multispectral"} or \code{"rgb"} (see
\code{\link[=build_odm_args]{build_odm_args()}}).}

\item{poll_seconds}{Status-poll interval in seconds. Default 60.}

\item{...}{Forwarded to \code{\link[=as_webodm_options]{as_webodm_options()}}.}
}
\value{
Invisibly returns a list with \code{task_id}, \code{final_status}, and
a named character vector of downloaded local paths.
}
\description{
Submits the project's MicaSense or aerial images to a WebODM server,
polls the task until completion, then downloads \code{orthophoto.tif},
\code{dsm.tif}, \code{dtm.tif} and \code{point_cloud.laz} (each only if produced)
into the project's expected ODM-shaped folders.
}
\details{
This is a blocking call: WebODM tasks can take many hours. Set
\code{poll_seconds} to control how often the function checks status.
}
\examples{
\dontrun{
project <- dronebio_project("/path/to/flight")
run_webodm_project(
  project,
  base_url = "http://localhost:8000",
  username = "admin",
  password = "secret",
  camera_type = "rgb",
  build_dsm = TRUE,
  build_dtm = TRUE
)
}
}
