% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster.R
\name{set_radiometric_level}
\alias{set_radiometric_level}
\alias{radiometric_level}
\title{Record or read the radiometric state of a raster}
\usage{
set_radiometric_level(x, level, scale_factor = NA_real_)

radiometric_level(x)
}
\arguments{
\item{x}{A \code{terra::SpatRaster}.}

\item{level}{One of the levels above.}

\item{scale_factor}{The divisor applied, when one was. \code{NA} otherwise.}
}
\value{
\code{set_radiometric_level()} returns \code{x} with the tags set.
\code{radiometric_level()} returns a length-1 character, \code{"unknown"} when the
raster carries no tag.
}
\description{
Dividing digital numbers by a constant does not produce reflectance, and
conflating the two is easy. These functions write the state into the
raster's metadata tags, where it survives a GeoTIFF round trip, so a
product can always say what was done to it.
}
\details{
The recognised levels are \code{"normalised_dn"} (DroneBioR divided by a
detected storage scale), \code{"engine_scaled"} (the raster already arrived in
0-1, carrying whatever the photogrammetry engine applied),
\code{"panel_calibrated"} (tied to a reflectance standard through panel regions
or a downwelling-light sensor, and the only level that is reflectance) and
\code{"unknown"}.
}
\examples{
r <- terra::rast(nrows = 4, ncols = 4)
terra::values(r) <- seq_len(16) * 1000
radiometric_level(scale_to_reflectance(r))
}
