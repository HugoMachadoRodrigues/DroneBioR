% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_project.R
\name{dronebio_sample_project}
\alias{dronebio_sample_project}
\title{Seed a clickable sample DroneBioR project from bundled fixtures}
\usage{
dronebio_sample_project(target_dir = file.path(tempdir(), "DroneBioR-sample"))
}
\arguments{
\item{target_dir}{Target project directory. Defaults to a stable folder
under \code{tempdir()} so multiple Shiny launches reuse the same seed.}
}
\value{
A \code{dronebio_project} pointing at \code{target_dir}.
}
\description{
Builds a working \code{dronebio_project} in \code{target_dir} and seeds it with the
synthetic fixtures shipped under \verb{inst/extdata/}. The seeded tree mirrors
the layout an OpenDroneMap run would produce, so downstream functions
(\code{read_multispectral_orthomosaic}, \code{build_chm_from_dsm_dtm},
\code{summarize_odm_products}, \code{run_dronebio_workflow}, the Shiny app) work
against it with no extra configuration.
}
\details{
Useful for clicking through the package and the Shiny app before you have
real flight data of your own. The fixtures are intentionally tiny
(32x32 pixel multispectral subset, ~17 KB total) and \strong{must not be used
for science}.

Files are copied with \code{overwrite = FALSE}, so re-running the function is
safe: it tops up missing files but does not clobber edits you have made
to the seeded folder.
}
\examples{
project <- dronebio_sample_project(target_dir = tempfile("dronebior-sample-"))
file.exists(project$odm_orthomosaic)
summarize_odm_products(project)
}
