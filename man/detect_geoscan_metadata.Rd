% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/odm_geo.R
\name{detect_geoscan_metadata}
\alias{detect_geoscan_metadata}
\title{Detect GeoScan metadata sibling of a source-images folder}
\usage{
detect_geoscan_metadata(images_dir, max_levels_up = 3L)
}
\arguments{
\item{images_dir}{Folder containing the drone JPGs / TIFFs.}

\item{max_levels_up}{How many parent directories to inspect for a
\verb{Metadata/} sibling. Default 3 covers the common
\verb{dataset/Images/*.JPG} layout.}
}
\value{
\code{NULL} when nothing was found, otherwise a list with
\code{cameras_path}, \code{gnss_offset_path}, \code{gcps_path} and \code{metadata_dir}.
Paths to non-existent files (e.g. \code{GNSS_offset.txt} when only
cameras are provided) are still returned so the caller can decide
what to do.
}
\description{
GeoScan datasets ship as \verb{<root>/Images/} (the JPGs) and
\verb{<root>/Metadata/} (the per-image camera positions, the GCPs and the
GNSS lever-arm offset). When \code{run_odm_project()} is pointed at the
images folder, this helper walks up to a few levels looking for a
\code{Metadata/Cameras_WGS84.txt} sibling and returns the paths it found.
}
\examples{
\dontrun{
  detect_geoscan_metadata("aerial_images_with_gcps/Images")
}
}
