% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{classify_ground_csf}
\alias{classify_ground_csf}
\title{Classify ground points in a LAS file using lidR's CSF algorithm}
\usage{
classify_ground_csf(
  las_path,
  sloop_smooth = FALSE,
  class_threshold = 0.5,
  cloth_resolution = 0.5,
  rigidness = 1L,
  ...
)
}
\arguments{
\item{las_path}{Path to a LAS or LAZ file.}

\item{sloop_smooth}{Logical, passed to \code{lidR::csf()}. Smooth before
building the simulated cloth.}

\item{class_threshold}{Numeric distance in meters from cloth to point;
below this, the point is ground.}

\item{cloth_resolution}{Numeric cloth grid resolution in meters.}

\item{rigidness}{Integer 1, 2, or 3. Cloth rigidness.}

\item{...}{Additional arguments passed to \code{lidR::csf()}.}
}
\value{
A classified \code{lidR::LAS} object.
}
\description{
Bridge to \code{lidR::classify_ground()} with the Cloth Simulation Filter
(Zhang et al., 2016). Returns a \code{lidR::LAS} object whose \code{Classification}
attribute marks ground points as 2 and the rest as 1.
}
\details{
Requires the optional \code{lidR} package (a Suggests dependency). The
function does not implement CSF in R itself - it just wraps the
upstream algorithm so users can stay inside the DroneBioR API surface.
}
\examples{
\dontrun{
las <- classify_ground_csf(
  "outputs/odm_micasense_dataset/micasense/odm_georeferencing/odm_georeferenced_model.las",
  class_threshold = 0.5,
  cloth_resolution = 0.5
)
}
}
