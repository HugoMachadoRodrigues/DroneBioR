% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{list_aerial_images}
\alias{list_aerial_images}
\title{List generic aerial images for an ODM project}
\usage{
list_aerial_images(images_dir)
}
\arguments{
\item{images_dir}{Folder containing raw image files.}
}
\value{
A data frame with the same columns as
\code{\link[=list_micasense_images]{list_micasense_images()}}: \code{file}, \code{filename}, \code{capture_id}, \code{band_id},
\code{file_size_mb}. For aerial RGB sets, \code{capture_id} is the base filename
without extension and \code{band_id} is always \code{1L}.
}
\description{
Permissive image lister for non-MicaSense flights (Sony RX1R, DJI Phantom
/ Mavic, Phase One, generic RGB). Returns a \code{list_micasense_images()}-
shaped manifest so \code{\link[=copy_images_for_odm]{copy_images_for_odm()}} and \code{\link[=run_odm_project]{run_odm_project()}} can
consume it transparently, but without enforcing the
\verb{^(.+)_([0-9]+)\\.[A-Za-z0-9]+$} capture/band filename pattern. Accepts
\code{.jpg}, \code{.jpeg}, \code{.png}, \code{.tif} and \code{.tiff} (case-insensitive).
}
\details{
\strong{DJI Mavic 3M datasets} drop their multispectral \verb{_MS_\{G,R,RE,NIR\}.TIF}
siblings from the returned manifest when at least one matching \verb{_D.JPG}
is also present. This keeps the existing \code{run_odm_project()} flow
viable (ODM only sees the RGB JPGs for SfM, which is what it can
actually handle), and the returned data frame gets an attribute
\code{dji_visible_multispectral = TRUE} so callers know the MS TIFs were
filtered out. For full DJI Mavic 3M processing — including the four
MS bands — use \code{\link[=list_dji_mavic_3m_images]{list_dji_mavic_3m_images()}} and
\code{\link[=run_odm_dji_mavic_3m]{run_odm_dji_mavic_3m()}}.
}
\examples{
tmp <- tempfile("aerial-"); dir.create(tmp)
for (i in 1:3) file.create(file.path(tmp, paste0("DJI_", sprintf("\%04d", i), ".JPG")))
head(list_aerial_images(tmp))
}
