% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biomass_mapping.R
\name{predict_biomass_map}
\alias{predict_biomass_map}
\title{Predict a biomass map from a fitted model}
\usage{
predict_biomass_map(
  model,
  grid,
  pasture = NULL,
  out_path = NULL,
  min_biomass = 0
)
}
\arguments{
\item{model}{A \code{dronebio_biomass_model}.}

\item{grid}{Predictor \code{SpatRaster} from \code{\link[=make_biomass_grid]{make_biomass_grid()}} - must contain
the model's predictor layers.}

\item{pasture}{Optional single pasture label to assign to every cell when
the model used a categorical pasture term (a map covers one pasture). Must
be one of the training levels.}

\item{out_path}{Optional GeoTIFF path to write.}

\item{min_biomass}{Lower clamp (kg/ha).}
}
\value{
The biomass \code{SpatRaster} (named \code{biomass_kgha}).
}
\description{
Applies a \code{\link[=fit_biomass_model]{fit_biomass_model()}} result across the predictor grid to produce
a per-cell above-ground biomass raster (kg/ha), clamped at \code{min_biomass}.
}
