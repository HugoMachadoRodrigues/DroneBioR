% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field_covariates.R
\name{build_prediction_stack}
\alias{build_prediction_stack}
\title{Build an aggregated covariate stack for map prediction}
\usage{
build_prediction_stack(
  reflectance,
  covariates,
  max_cells = 1e+06,
  window = 1L,
  custom_index = NULL,
  chm = NULL,
  dsm = NULL,
  dtm = NULL
)
}
\arguments{
\item{reflectance}{Reflectance \code{SpatRaster}.}

\item{covariates}{Covariate ids to supply, in order (\code{model$predictors}).}

\item{max_cells}{Approximate cell budget for the aggregated grid.}

\item{window}{Extraction window the model was trained with.}

\item{custom_index}{Optional custom index \code{SpatRaster}.}

\item{chm, dsm, dtm}{Optional terrain \code{SpatRaster}s.}
}
\value{
A \code{SpatRaster} whose \code{names()} are exactly \code{covariates}, in order,
with \code{fact} and \code{cell_size_m} attributes.
}
\description{
The fast map path: aggregate the reflectance first, then compute indices,
proxies and terrain layers on the small grid. \code{fact} defaults to the
training window whenever the cell budget allows, so one map cell matches
the support the model was trained on.
}
\details{
\strong{Approximation:} indices here come from block-mean reflectance, whereas
training used the mean of native-resolution indices. The two differ
slightly because the index formulas are nonlinear. Use
\code{\link[=export_field_biomass_map]{export_field_biomass_map()}} for the exact surface.
}
\examples{
ortho <- system.file("extdata", "micasense_subset.tif", package = "DroneBioR")
refl <- scale_to_reflectance(read_multispectral_orthomosaic(ortho)$bands)
stack <- build_prediction_stack(refl, c("NDVI", "NDRE"), max_cells = 500)
names(stack)
}
