% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biomass_mapping.R
\name{run_biomass_mapping}
\alias{run_biomass_mapping}
\title{Run the field-calibrated biomass mapping workflow}
\usage{
run_biomass_mapping(
  field,
  indices,
  chm = NULL,
  pasture = NULL,
  out_path = NULL,
  grid_m = 1,
  method = c("auto", "lm", "rf"),
  predictors = NULL,
  buffer_m = NULL,
  min_biomass = 0
)
}
\arguments{
\item{field}{Field data frame or CSV path (see \code{\link[=read_field_data]{read_field_data()}}). Needs
\code{biomass_kgha} on the clip rows; \code{plate_height_cm} on all rows enables the
plate-meter step; a \code{pasture} column enables the categorical RF term.}

\item{indices}{Spectral index \code{SpatRaster} from \code{\link[=compute_spectral_indices]{compute_spectral_indices()}}.}

\item{chm}{Optional CHM \code{SpatRaster}.}

\item{pasture}{Optional pasture label for the mapped raster (defaults to the
single pasture present in \code{field}).}

\item{out_path}{Optional GeoTIFF path for the biomass map.}

\item{grid_m}{Management grid-cell size (m).}

\item{method}{Model route: \code{"lm"}, \code{"rf"}, or \code{"auto"}.}

\item{predictors}{Optional predictor override.}

\item{buffer_m}{Optional buffer radius for calibration extraction.}

\item{min_biomass}{Lower clamp (kg/ha) for the map.}
}
\value{
A list with \code{plate_meter}, \code{grid}, \code{calibration}, \code{model}, \code{map},
\code{metrics} and \code{map_path}.
}
\description{
End-to-end driver: read field samples, calibrate the plate meter, build the
predictor grid, assemble the calibration table, fit the staged model and
write a biomass map. Returns every intermediate so a script or the Shiny
studio can report on them.
}
\examples{
\dontrun{
  res <- run_biomass_mapping(
    field   = "field_biomass_plate.csv",
    indices = ix, chm = chm,
    out_path = "biomass_kgha.tif", method = "auto"
  )
  res$model
}
}
