% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field_ingest.R
\name{field_source_columns}
\alias{field_source_columns}
\title{Probe a field-sample file for columns, CRS and a column-mapping guess}
\usage{
field_source_columns(path, layer = NULL)
}
\arguments{
\item{path}{Path to a CSV or vector dataset.}

\item{layer}{Optional layer name for multi-layer sources (GeoPackage).}
}
\value{
A list with \code{kind} (\code{"table"} or \code{"vector"}), \code{columns},
\code{classes}, \code{n_missing}, \code{n_rows}, \code{crs}, \code{epsg}, \code{has_geometry},
\code{geom_type} and \code{guess} (\code{id} / \code{biomass} / \code{x} / \code{y}).
}
\description{
Reads only what is needed to populate the column-mapping wizard. The
returned \code{columns} are the names as they actually come back from the
driver - DBF truncates to 10 characters, so downstream code must never
match hard-coded names.
}
\examples{
path <- system.file("extdata", "field_samples.csv", package = "DroneBioR")
probe <- field_source_columns(path)
probe$kind
probe$guess$biomass
}
