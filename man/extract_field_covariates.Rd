% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field_extract.R
\name{extract_field_covariates}
\alias{extract_field_covariates}
\title{Extract windowed covariates at field sample points}
\usage{
extract_field_covariates(
  points,
  reflectance,
  selected,
  window = 1L,
  window_m = NULL,
  fun = "mean",
  custom_index = NULL,
  chm = NULL,
  dsm = NULL,
  dtm = NULL
)
}
\arguments{
\item{points}{Field samples from \code{\link[=prepare_field_table]{prepare_field_table()}} (or any \code{sf}
POINT layer).}

\item{reflectance}{Reflectance \code{SpatRaster} defining the grid.}

\item{selected}{Covariate ids to extract, in order (see
\code{\link[=field_covariate_catalogue]{field_covariate_catalogue()}}).}

\item{window}{Odd window size (see \code{\link[=window_cells]{window_cells()}}).}

\item{window_m}{Support in metres instead of pixels. A window in pixels
spans a different area at every ground sampling distance, so a quadrat
size is better stated in metres and converted per survey: this rounds to
the nearest odd pixel count for \code{reflectance} and errors when the request
falls outside the supported range. See \code{\link[=window_from_metres]{window_from_metres()}}.}

\item{fun}{Aggregation: \code{"mean"}, \code{"median"}, \code{"max"}, \code{"min"}, \code{"sd"}.}

\item{custom_index}{Optional single-layer \code{SpatRaster} with a user index.}

\item{chm, dsm, dtm}{Optional terrain \code{SpatRaster}s.}
}
\value{
A plain data frame with one row per input point, in input order:
every original attribute, then one numeric column per \code{selected} id in
that order, then \code{.n_valid_px}, \code{.window_px}, \code{.window_valid_frac} and
\code{.in_extent}. Out-of-extent points keep their row with all-\code{NA}
covariates. Carries \code{window_px}, \code{window_fun}, \code{crs} and
\code{reference_geom} attributes describing the training grid.
}
\description{
The extraction pipeline for the Field Models tab. A covariate value at a
field point is the aggregate (\code{fun}) over the \code{window} x \code{window} block of
\strong{per-pixel covariate values computed at native resolution} - indices are
never computed from block means during training.
}
\details{
Terrain layers and a custom index are sampled bilinearly at the window
pixel centres, which is mathematically identical to resample-then-extract
and avoids a whole-raster \code{terra::resample()}.
}
\examples{
ortho <- system.file("extdata", "micasense_subset.tif", package = "DroneBioR")
refl <- scale_to_reflectance(read_multispectral_orthomosaic(ortho)$bands)
pts <- read_field_points(
  system.file("extdata", "field_samples.csv", package = "DroneBioR"),
  crs = 32617
)
tab <- extract_field_covariates(pts, refl, c("NDVI", "NDRE"), window = 3)
head(tab[, c("sample_id", "NDVI", "NDRE", ".n_valid_px")])
}
