% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine_odm.R
\name{rebuild_from_edited_cloud}
\alias{rebuild_from_edited_cloud}
\title{Build the ODM products from an edited point cloud}
\usage{
rebuild_from_edited_cloud(project, ...)
}
\arguments{
\item{project}{A \code{dronebio_project} with a finished (or at least
past-\code{odm_filterpoints}) run.}

\item{...}{Passed to \code{\link[=run_odm_project]{run_odm_project()}}, e.g. \code{build_dsm}, \code{build_dtm},
\code{camera_type}.}
}
\value{
Invisibly, the list returned by \code{\link[=run_odm_project]{run_odm_project()}}.
}
\description{
Reruns an existing ODM project from \code{odm_meshing}, so the mesh, texture,
DEMs and orthophoto are rebuilt from whatever
\code{odm_filterpoints/point_cloud.ply} currently holds -- including a cloud you
cleaned by hand with \code{\link[=write_ply_subset]{write_ply_subset()}} or the app's 3D editor.
}
\details{
This works because ODM hands that single file from \code{odm_filterpoints} to
\code{odm_meshing}, and \code{odm_filterpoints} skips itself when the file already
exists and it is not the stage being rerun. Starting at \code{odm_meshing}
therefore leaves the edited cloud untouched, while \code{opensfm} and \code{openmvs}
-- the bulk of the runtime -- are reused.
}
\examples{
\dontrun{
p <- dronebio_project("~/flights/2026-05-01")
ply <- file.path(p$odm_project_dir, "odm_filterpoints", "point_cloud.ply")
pc <- read_ply_point_cloud(ply, max_points = 5e5)
keep <- setdiff(seq_len(parse_ply_header(ply)$n_vertices),
                pc$point_id[pc$z > quantile(pc$z, 0.999)])
write_ply_subset(ply, ply, keep = keep)
rebuild_from_edited_cloud(p, build_dsm = TRUE, build_dtm = TRUE)
}
}
