% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_helpers.R
\name{observer_need}
\alias{observer_need}
\title{Guard an observer, showing the reason when it stops}
\usage{
observer_need(
  cond,
  message,
  type = "warning",
  duration = 10,
  session = NULL
)
}
\arguments{
\item{cond}{Condition that must hold. Evaluated with \code{\link[shiny:isTruthy]{shiny::isTruthy()}}, so
the semantics match \code{\link[shiny:need]{shiny::need()}}.}

\item{message}{Message shown when \code{cond} does not hold.}

\item{type}{Notification type, as in \code{\link[shiny:showNotification]{shiny::showNotification()}}.}

\item{duration}{Seconds the notification stays up.}

\item{session}{Shiny session. Defaults to the current reactive domain.}
}
\value{
\code{invisible(TRUE)} when \code{cond} holds; otherwise aborts the observer.
}
\description{
\code{validate(need(...))} is the right guard inside \code{render*()} and \code{reactive()}:
Shiny catches the \code{shiny.silent.error} it raises and displays the message in
the dependent output. Inside \code{observeEvent()} there is no output to display
it in, so Shiny swallows the condition and the click produces nothing at
all: no toast, no console message, just a button that looks broken.
}
\details{
This raises the same abort, but shows the reason first. Use it in place of
\code{validate(need())} in any observer.
}
\examples{
\dontrun{
observeEvent(input$train, {
  observer_need(nrow(samples()) > 0, "Extract field samples before training.")
  train_models(samples())
})
}
}
