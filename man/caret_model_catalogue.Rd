% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field_caret.R
\name{caret_model_catalogue}
\alias{caret_model_catalogue}
\title{Catalogue caret models of a given type}
\usage{
caret_model_catalogue(
  type = "Regression",
  installed = rownames(utils::installed.packages())
)
}
\arguments{
\item{type}{caret model type, normally \code{"Regression"}.}

\item{installed}{Character vector of installed package names.}
}
\value{
A data frame with \code{method}, \code{label}, \code{packages}, \code{missing},
\code{ready}, \code{tags}, \code{n_params} and \code{needs_scaling}. Zero rows (with a
warning) when caret is not installed.
}
\description{
All 137 regression-capable caret models are offered; the remaining 102 are
classification-only and cannot fit a continuous biomass response. \code{ready}
says whether every backend package the model needs is already installed,
which is what the picker groups on.
}
\examples{
if (requireNamespace("caret", quietly = TRUE)) {
  cat <- caret_model_catalogue()
  nrow(cat)
  head(cat[cat$ready, c("method", "label")])
}
}
