% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field_covariates.R
\name{biomass_map_breaks}
\alias{biomass_map_breaks}
\title{Quantile breaks and a robust stretch for a biomass map}
\usage{
biomass_map_breaks(map, n = 4L, sample_size = 1e+05)
}
\arguments{
\item{map}{Single-layer biomass \code{SpatRaster}.}

\item{n}{Number of classes.}

\item{sample_size}{Cell budget for \code{terra::spatSample()}.}
}
\value{
A list with \code{breaks} (length \code{n + 1}), \code{quartiles}, \code{p01}, \code{p99}
and \code{labels}.
}
\description{
Large maps are sampled rather than read in full; regular sampling
reproduces the full-raster quantiles closely enough for a legend and
costs a fraction of the I/O.
}
\examples{
r <- terra::rast(nrows = 20, ncols = 20)
terra::values(r) <- seq_len(terra::ncell(r))
biomass_map_breaks(r, n = 4)$breaks
}
