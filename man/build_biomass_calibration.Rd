% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/biomass_mapping.R
\name{build_biomass_calibration}
\alias{build_biomass_calibration}
\title{Assemble the biomass calibration table}
\usage{
build_biomass_calibration(
  field,
  grid,
  plate_meter = NULL,
  biomass = "biomass_kgha",
  height = "plate_height_cm",
  group = "pasture",
  id = "sample_id",
  buffer_m = NULL
)
}
\arguments{
\item{field}{Field data frame or path to a CSV understood by
\code{\link[=read_field_data]{read_field_data()}}.}

\item{grid}{Predictor \code{SpatRaster} from \code{\link[=make_biomass_grid]{make_biomass_grid()}}.}

\item{plate_meter}{Optional \code{dronebio_plate_meter}; when supplied, rows with
a finite height but missing biomass are filled with its prediction.}

\item{biomass, height, group, id}{Column names in \code{field}.}

\item{buffer_m}{Optional radius (m). When set, predictors are the mean over
a circular buffer instead of the single containing cell - useful when GPS
error spans more than one grid cell.}
}
\value{
A data frame with the field columns, a \code{biomass_source} flag
(\code{measured} / \code{plate_modeled}) and one column per predictor.
}
\description{
Reads georeferenced field points, optionally fills the biomass of
plate-only points from a \code{\link[=fit_plate_meter]{fit_plate_meter()}} calibration, and joins each
point to the predictor grid cell it falls in (or the mean of a buffer
around it). The result is one tidy table ready for \code{\link[=fit_biomass_model]{fit_biomass_model()}}.
}
