% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pointcloud.R
\name{derive_tree_candidates}
\alias{derive_tree_candidates}
\title{Derive approximate tree candidates from a point cloud}
\usage{
derive_tree_candidates(
  points,
  grid_size = 4,
  min_height = 1.5,
  min_points = 5,
  max_trees = 80
)
}
\arguments{
\item{points}{Data frame from \code{read_ply_point_cloud()}.}

\item{grid_size}{Grid size in source coordinate units.}

\item{min_height}{Minimum height above local ground proxy.}

\item{min_points}{Minimum number of points per candidate.}

\item{max_trees}{Maximum number of candidates to return.}
}
\value{
A data frame of approximate tree objects.
}
\description{
This is a preview-grade canopy object detector. It bins high points into a
regular grid and estimates height, crown diameter and crown volume. Scientific
tree metrics should later use a CHM from DSM-DTM and validated segmentation.
}
\examples{
set.seed(1)
n <- 300
pts <- data.frame(
  x = c(rnorm(n/3, 5, 0.5), rnorm(n/3, 15, 0.5), rnorm(n/3, 25, 0.5)),
  y = c(rnorm(n/3, 5, 0.5), rnorm(n/3, 5, 0.5), rnorm(n/3, 15, 0.5)),
  z = c(rnorm(n/3, 55, 0.2), rnorm(n/3, 57, 0.2), rnorm(n/3, 54, 0.2))
)
derive_tree_candidates(pts)
}
