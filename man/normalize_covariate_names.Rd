% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field_covariates.R
\name{normalize_covariate_names}
\alias{normalize_covariate_names}
\title{Canonicalise covariate / layer names}
\usage{
normalize_covariate_names(x, warn = TRUE)
}
\arguments{
\item{x}{Character vector of names.}

\item{warn}{Emit a warning listing the renames.}
}
\value{
A named character vector mapping each original name to its
normalised form.
}
\description{
Applied identically to the training frame and to every prediction stack,
so a CHM that arrived as \code{CHM}, \code{chm} or \code{CHM_m} becomes one name and
\code{terra::predict()} never fails on a mismatch.
}
\details{
True duplicates are an error rather than a silent rename: a stack with two
layers called \code{CHM_m} would make \code{terra::predict()} fail much later with
\verb{duplicate names in SpatRaster}.
}
\examples{
normalize_covariate_names(c("NDVI", "chm", "dsm"), warn = FALSE)
}
