% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster.R
\name{orthomosaic_band_presence}
\alias{orthomosaic_band_presence}
\title{Which spectral bands an orthomosaic actually carries}
\usage{
orthomosaic_band_presence(x, nlyr = NULL)
}
\arguments{
\item{x}{A \code{SpatRaster}, or a character vector of layer names.}

\item{nlyr}{Layer count, used only as a fallback when \code{x} carries no
recognisable band names. Taken from \code{x} when it is a \code{SpatRaster}.}
}
\value{
A list with \code{has_nir}, \code{has_rededge} and \code{by}, the last being
\code{"name"} or \code{"count"} depending on which signal was used.
}
\description{
Decides whether NIR and RedEdge are present from the layer \emph{names} a
raster declares, falling back to the layer count only when the file was
written without informative names.
}
\details{
Counting layers alone is not enough, and getting this wrong is expensive:
it silently hides NDVI, NDRE, EVI and every other multispectral index from
a flight that has the bands. A MicaSense orthomosaic labels its bands
\verb{Red, Green, Blue, NIR, Rededge} (plus alpha), and a DJI Mavic 3M stack
likewise, so the names are the reliable signal; a 4-band RGB + alpha file
and a 4-band multispectral subset are indistinguishable by count.
}
\examples{
orthomosaic_band_presence(c("Red", "Green", "Blue", "NIR", "Rededge"))
orthomosaic_band_presence(c("red", "green", "blue"), nlyr = 3)
}
