% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field_covariates.R
\name{classify_biomass_map}
\alias{classify_biomass_map}
\title{Classify a biomass map into labelled classes}
\usage{
classify_biomass_map(map, breaks, labels = NULL)
}
\arguments{
\item{map}{Single-layer biomass \code{SpatRaster}.}

\item{breaks}{Cut values, as returned by \code{\link[=biomass_map_breaks]{biomass_map_breaks()}}.}

\item{labels}{Optional class labels; defaults to \code{"lo - hi"} ranges.}
}
\value{
A categorical \code{SpatRaster} named \code{biomass_class}.
}
\description{
Classify a biomass map into labelled classes
}
\examples{
r <- terra::rast(nrows = 20, ncols = 20)
terra::values(r) <- seq_len(terra::ncell(r))
brk <- biomass_map_breaks(r, n = 4)
terra::freq(classify_biomass_map(r, brk$breaks))
}
