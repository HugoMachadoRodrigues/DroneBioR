% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field_ingest.R
\name{read_field_points}
\alias{read_field_points}
\title{Read field sample points from a CSV or vector file}
\usage{
read_field_points(path, crs = NULL, x_col = NULL, y_col = NULL, layer = NULL)
}
\arguments{
\item{path}{Path to a CSV or vector dataset (see \code{\link[=stage_uploaded_vector]{stage_uploaded_vector()}}).}

\item{crs}{CRS of the coordinates: an EPSG code, a WKT / PROJ string, or
\code{NULL}. Required for CSV input, and used for a vector file whose CRS is
undefined (a shapefile with no \code{.prj}).}

\item{x_col, y_col}{Coordinate columns for CSV input. Guessed with
\code{\link[=field_source_columns]{field_source_columns()}} when \code{NULL}.}

\item{layer}{Optional layer name for multi-layer sources.}
}
\value{
An \code{sf} POINT layer. When polygons were reduced to centroids the
result carries a \code{centroid_note} attribute.
}
\description{
The single ingest entry point for the Field Models tab. Column names are
never renamed here - see \code{\link[=prepare_field_table]{prepare_field_table()}} for that.
}
\details{
A CSV \strong{must} be given a \code{crs}: bare \code{x} / \code{y} numbers carry no spatial
meaning, and adopting the orthomosaic's CRS for what are actually
longitude / latitude degrees produces silently wrong extractions.
}
\examples{
path <- system.file("extdata", "field_samples.csv", package = "DroneBioR")
pts <- read_field_points(path, crs = 32617)
nrow(pts)
}
