% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field_extract.R
\name{synthesize_pixel_raster}
\alias{synthesize_pixel_raster}
\title{Pack pixel values into a one-row SpatRaster}
\usage{
synthesize_pixel_raster(values, crs = "")
}
\arguments{
\item{values}{Numeric matrix or data frame; one column per layer, one row
per pixel. Column names become layer names.}

\item{crs}{CRS string for the synthetic grid. Any consistent value works -
it only has to match between stacks that will be compared with
\code{terra::compareGeom()}.}
}
\value{
A \code{terra::SpatRaster} with 1 row, \code{nrow(values)} columns and
\code{ncol(values)} layers.
}
\description{
Lets per-pixel raster functions (\code{\link[=compute_spectral_indices]{compute_spectral_indices()}},
\code{\link[=compute_biomass_proxies]{compute_biomass_proxies()}}) run on a handful of window pixels instead of
hundreds of millions of cells. terra fills row-major, so cell \emph{i} is row
\emph{i} of \code{values} and the input order is preserved exactly.
}
\examples{
r <- synthesize_pixel_raster(data.frame(Red = c(0.1, 0.2), NIR = c(0.5, 0.6)))
dim(r)
terra::values(r)
}
