% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine_webodm.R
\name{as_webodm_options}
\alias{as_webodm_options}
\title{Translate \code{build_odm_args()} arguments into a WebODM options list}
\usage{
as_webodm_options(
  camera_type = c("multispectral", "rgb"),
  orthophoto_resolution_cm = 5,
  fast_orthophoto = TRUE,
  build_dsm = FALSE,
  build_dtm = FALSE,
  pc_las = FALSE,
  pc_copc = FALSE,
  pc_csv = FALSE,
  tiles = FALSE,
  three_d_tiles = FALSE,
  gltf = FALSE,
  extra = list()
)
}
\arguments{
\item{camera_type}{\code{"multispectral"} or \code{"rgb"}. Sets
\code{radiometric-calibration = "camera+sun"} for multispectral; omits
it for RGB.}

\item{orthophoto_resolution_cm}{Orthophoto resolution (cm).}

\item{fast_orthophoto, build_dsm, build_dtm, pc_las, pc_copc, pc_csv, tiles, three_d_tiles, gltf}{Same semantics as \code{build_odm_args()}.}

\item{extra}{A named list of additional WebODM options to merge.}
}
\value{
A named list with one entry per WebODM option, ready for
\code{\link[=webodm_submit_task]{webodm_submit_task()}}.
}
\description{
WebODM's \verb{/api/projects/\{id\}/tasks/} endpoint takes options as
\verb{[\{"name": "dsm", "value": true\}, ...]}. This helper maps the same
boolean / numeric flags the \code{build_odm_args()} ODM-CLI driver uses
so a Shiny form can drive either engine from one set of inputs.
}
