% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dji_ppk.R
\name{ppk_cli_rtklib_dji}
\alias{ppk_cli_rtklib_dji}
\title{PPK CLI hook factory using rtklib + a DJI .bin -> RINEX converter}
\usage{
ppk_cli_rtklib_dji(
  base_obs_path,
  dji_bin_to_rinex_cmd,
  rnx2rtkp_cmd = "rnx2rtkp",
  rnx2rtkp_extra = character()
)
}
\arguments{
\item{base_obs_path}{Path to the base station RINEX observation
file. Required.}

\item{dji_bin_to_rinex_cmd}{Command (name or full path) that
converts a DJI Mavic 3M \verb{_PPKRAW.bin} to RINEX. Must accept
the .bin path as its first positional argument and write the
RINEX \code{.obs} to its standard output \strong{or} to a file named
\verb{<bin>.obs}.}

\item{rnx2rtkp_cmd}{Command for rtklib's PPK runner. Default
\code{"rnx2rtkp"}.}

\item{rnx2rtkp_extra}{Extra arguments forwarded to \code{rnx2rtkp}.
For example \code{c("-p", "0")} to force static positioning, or
\code{c("-c", "/path/to/config.conf")} for a custom configuration.}
}
\value{
A function with signature
\verb{function(images_dir, bin_paths, nav_paths, mrk_paths)},
ready to pass as \code{ppk_cli} to \code{\link[=run_odm_dji_mavic_3m]{run_odm_dji_mavic_3m()}}.
}
\description{
Returns a function suitable for the \code{ppk_cli} argument of
\code{\link[=run_odm_dji_mavic_3m]{run_odm_dji_mavic_3m()}}. When called, the function:
}
\details{
\enumerate{
\item Converts the DJI \verb{_PPKRAW.bin} rover observations into RINEX
using \code{dji_bin_to_rinex_cmd} (a user-supplied CLI that knows
DJI's proprietary binary; \strong{rtklib's \code{convbin} does NOT
handle DJI .bin out of the box}, so this must be a tool the
user installed separately — for example
\href{https://klauppk.com/}{KlauPPK} or any DJI
Smart Farm-compatible converter).
\item Runs rtklib's \code{rnx2rtkp} with the rover RINEX, the
\verb{_PPKNAV.nav} ephemerides and the \strong{user-supplied base
station RINEX} to produce a positioning solution.
\item Rewrites each \verb{_Timestamp.MRK} in place with the corrected
lat / lon / alt per trigger event by matching the .MRK GPS
time-of-week column to the solution's timestamps.
}

If any step fails the hook emits a \code{warning()} and leaves the
.MRK files unchanged; \code{\link[=run_odm_dji_mavic_3m]{run_odm_dji_mavic_3m()}} then falls back to
the .MRK-as-shipped path.

Tooling expectations:
\itemize{
\item \code{rnx2rtkp} must be on \code{PATH}. On macOS: \verb{brew install rtklib}.
\item \code{dji_bin_to_rinex_cmd} must be on \code{PATH} or a full path. Pass
the user-installed converter's name explicitly — there is no
universal default.
\item \code{base_obs_path} is the base station RINEX observation file
(\code{.YYo} / \code{.obs}). The user typically downloads this from a
public CORS network for the flight day + a base receiver
covering the survey area.
}
}
\examples{
\dontrun{
  hook <- ppk_cli_rtklib_dji(
    base_obs_path        = "~/ppk/base_2026_05_01.26o",
    dji_bin_to_rinex_cmd = "klauppk_dji_to_rinex"
  )
  run_odm_dji_mavic_3m(project, ppk_cli = hook)
}
}
