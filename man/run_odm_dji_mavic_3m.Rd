% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dji_mavic_3m.R
\name{run_odm_dji_mavic_3m}
\alias{run_odm_dji_mavic_3m}
\title{Run OpenDroneMap on a DJI Mavic 3M flight, producing a 7-band ortho}
\usage{
run_odm_dji_mavic_3m(
  project,
  force = FALSE,
  odm_image = "opendronemap/odm",
  orthophoto_resolution_cm = 5,
  max_concurrency = NULL,
  ms_mode = c("multispectral", "per_band"),
  primary_band = "auto",
  build_dsm = TRUE,
  build_dtm = TRUE,
  fast_orthophoto = FALSE,
  auto_boundary = TRUE,
  pc_las = FALSE,
  skip_3dmodel = TRUE,
  skip_report = TRUE,
  cleanup_intermediates = TRUE,
  harmonize = TRUE,
  canopy_ceiling = 18,
  use_ppk_mrk = TRUE,
  ppk_min_fix_quality = 4L,
  ppk_cli = "auto",
  rgb_extra_args = character(),
  ms_extra_args = character()
)
}
\arguments{
\item{project}{A \code{dronebio_project} object whose \code{images_dir}
contains DJI Mavic 3M raw images.}

\item{force}{Logical. Re-run every band even if outputs already
exist. Useful after changing camera or radiometric parameters.}

\item{odm_image}{Docker image tag for the ODM container.}

\item{orthophoto_resolution_cm}{Orthophoto ground sampling distance.}

\item{max_concurrency}{Concurrent ODM workers per band. \code{NULL}
(default) auto-detects the machine's physical cores.}

\item{ms_mode}{One of \code{"multispectral"} (default) or \code{"per_band"}.
In \code{"multispectral"} mode the four \verb{_MS_*.TIF} bands are processed
in a \strong{single} ODM run: ODM reads each image's DJI band metadata
(\code{BandName}, \code{RigCameraIndex}, \code{CentralWavelength}) to group the
bands by capture, reconstructs once, and co-registers the bands —
so the resulting orthomosaic is pixel-aligned across bands and the
spectral indices come out clean with identical coverage. This is
only 2 ODM runs total (RGB + MS). In the legacy \code{"per_band"} mode
each MS band gets its own independent ODM run (5 runs total); the
band orthos then end up mis-registered, which yields noisy indices
and different valid-data coverage per index. Use \code{"per_band"} only
if ODM fails to recognise the multispectral grouping on your data.}

\item{primary_band}{Multispectral mode only. The band ODM uses to
drive the reconstruction, passed as \code{--primary-band}. \code{"auto"}
(default) lets ODM choose. Override with a band name (e.g.
\code{"NIR"}) if the auto choice reconstructs poorly.}

\item{build_dsm, build_dtm}{Logical, default \code{TRUE}. Build the DSM /
DTM on the RGB run. Set both to \code{FALSE} when you only need the
orthomosaic + spectral indices — combined with
\code{fast_orthophoto = TRUE} this is the fastest path.}

\item{fast_orthophoto}{Logical, default \code{FALSE}. When \code{TRUE}, the
RGB run adds ODM's \code{--fast-orthophoto}, which skips the dense
MVS reconstruction (often the single longest stage). The
orthomosaic is built from the 2.5D mesh instead — much faster,
but any DSM / DTM produced alongside are lower quality. Leave
\code{FALSE} for scientifically defensible canopy heights.}

\item{auto_boundary}{Logical, default \code{TRUE}. Adds ODM's
\code{--auto-boundary}, which crops the reconstruction and — crucially
— the orthophoto canvas to a polygon derived from the camera GPS.
This prevents a common failure on bounded aerial surveys where a
few stray, mis-registered points scatter kilometres from the true
footprint: without cropping, the orthophoto stage tries to render
that whole sprawl at full resolution and the Docker container is
OOM-killed (exit 137) even though the cropped DSM / DTM wrote
fine. Set \code{FALSE} only if your survey genuinely has no usable
camera GPS.}

\item{pc_las}{Logical. When \code{TRUE}, export the dense point cloud
from the RGB run as a \code{.las} file (~640 MB for a 300-image
flight). Default \code{FALSE} — DroneBioR's DSM/DTM/CHM pipeline
does not need the LAS. Set to \code{TRUE} if you plan to run
\code{improve_dtm_csf()} afterwards (which reads the LAS).}

\item{skip_3dmodel}{Logical. Default \code{TRUE}. Adds \verb{--skip-3dmodel}
to every ODM invocation so the \code{odm_meshing} and \code{mvs_texturing}
stages — which together cost 10-30 min per flight and only
produce a textured \code{.obj} / \code{.glb} 3D model that DroneBioR's
spectral pipeline never reads — are skipped. Set to \code{FALSE} if
you want the textured 3D model for visualization.}

\item{skip_report}{Logical. Default \code{TRUE}. Adds \code{--skip-report}
so ODM does not generate its PDF run report. Saves ~1-2 min
per band and avoids the well-known \code{gdal_translate} / numpy
ABI crash inside some \code{opendronemap/odm} Docker images.}

\item{cleanup_intermediates}{Logical. Default \code{TRUE}. After the
7-band stacked orthomosaic is written, perform two cleanups
so the user is left with only the products DroneBioR's
downstream pipeline actually consumes:
\itemize{
\item Delete the per-MS-band ODM project directories
(\verb{dji_ms_g/}, \verb{dji_ms_r/}, \verb{dji_ms_re/}, \verb{dji_ms_nir/}).
The per-band orthos already live in the 7-band stack.
\item Inside the canonical RGB project folder (\verb{dji/}),
strip every directory and file except \verb{odm_dem/}
(DSM/DTM, plus CHM later), \verb{odm_orthophoto/} (RGB ortho
+ 7-band DJI stack), \code{log.json} (ODM's log) and
\code{dronebior_odm.log} (our docker output). The discarded
intermediates — \verb{images/}, \verb{opensfm/}, \verb{openmvs/},
\verb{odm_filterpoints/}, \verb{odm_georeferencing/},
\verb{odm_postprocess/} and the small JSON/TXT bookkeeping
files — are never read by the downstream R pipeline.
}
Set \code{FALSE} to keep everything for debugging.}

\item{harmonize}{Logical, default \code{TRUE}. After the run, make the
DSM, DTM and CHM physically consistent and spike-free in place via
\code{\link[=harmonize_dem_products]{harmonize_dem_products()}}: the DTM is despiked, the CHM is
derived non-negative and despiked, and the DSM is rebuilt as
\code{DTM + CHM} so \code{CHM >= 0} and \code{DSM >= DTM} hold everywhere. The
raw ODM rasters are preserved as \code{dsm_raw.tif} / \code{dtm_raw.tif}, and
the canonical \code{dsm.tif} / \code{dtm.tif} / \code{chm.tif} become the clean
versions so every downstream consumer (indices, Shiny app,
\code{build_chm_raster()}) uses them transparently.}

\item{canopy_ceiling}{Height (m) above the local canopy beyond which
a cell is treated as a reconstruction tower during harmonization.
Default \code{18} (pasture with scattered trees). Raise for forest,
lower for open pasture.}

\item{use_ppk_mrk}{Logical. Default \code{TRUE}. When the source folder
carries the DJI \verb{_Timestamp.MRK} sidecar(s), parse them with
\code{\link[=parse_djim3m_mrk_folder]{parse_djim3m_mrk_folder()}}, resolve each per-band filename to
its photo number, and write an ODM \code{geo.txt} so ODM uses the
RTK / PPK positions instead of the EXIF GPS (which the Mavic 3M
notoriously corrupts on altitude — that is the bug that makes
OpenSfM diverge and \code{odm_orthophoto} OOM). ODM then runs with
\verb{--geo /datasets/<proj>/geo.txt --gps-accuracy 0.10}.}

\item{ppk_min_fix_quality}{Integer. Default \code{4} (RTK Float).
Photos whose .MRK row reports a lower fix quality are dropped
from the geo.txt — including them would let degraded positions
destabilise the bundle adjustment. Set to \code{50} to demand
RTK-Fixed-only, or \code{0} to keep everything.}

\item{ppk_cli}{Controls the \strong{PPK CLI step that runs before ODM}
when the source folder ships the \code{.bin} / \code{.nav} rover files.
Three forms are accepted:
\describe{
\item{\code{"auto"} (default)}{Probe the system for everything
\code{ppk_cli_rtklib_dji()} needs: \code{rnx2rtkp} on PATH (e.g.
\verb{brew install rtklib}), a DJI \code{.bin} -> RINEX converter
on PATH (tried in order: \code{klauppk_dji_to_rinex},
\code{klauppk}, \code{dji_to_rinex}, \code{djiparsekit},
\code{djirinexconverter}, \code{convbin}), and a base-station
RINEX observation file located via (a) the
\code{DRONEBIOR_PPK_BASE_OBS} environment variable, (b) the
\code{dronebior.ppk_base_obs} R option, or (c)
\verb{<images_dir>/base/*.obs|*.YYo}. When every piece is in
place, run full PPK before ODM. When anything is missing,
emit a clear message naming what is missing and fall back
to the .MRK-as-shipped path.}
\item{\code{NULL} / \code{FALSE}}{Skip the CLI step. Use the .MRK as
it ships from the drone (still better than EXIF GPS
because the .MRK already holds RTK-quality positions when
the drone had an RTK Fix in flight).}
\item{A function}{Custom hook with signature
\verb{function(images_dir, bin_paths, nav_paths, mrk_paths)}.
Use \code{\link[=ppk_cli_rtklib_dji]{ppk_cli_rtklib_dji()}} to build one with explicit
paths if the auto-detect probes need an override.}
}
In every case DroneBioR then reads the (possibly improved)
.MRK and writes an ODM \code{geo.txt} consumed via \code{--geo}.}

\item{rgb_extra_args}{Extra arguments appended to the \strong{RGB} ODM
run (\code{build_odm_args(..., extra_args = ...)}).}

\item{ms_extra_args}{Extra arguments appended to \strong{each MS} ODM
run.}
}
\value{
A list with paths to the per-band orthos, the RGB DSM /
DTM, and the stacked 7-band orthomosaic.
}
\description{
DJI Mavic 3M captures 1 RGB (\verb{_D.JPG}) and 4 single-band
multispectral TIFFs (\verb{_MS_G/R/RE/NIR.TIF}) per shot — five
independent cameras from ODM's perspective. ODM cannot bundle-adjust
the burst as one capture, so this function orchestrates \strong{five
separate ODM runs}: one on the RGB JPGs (full pipeline -
orthomosaic, DSM, DTM, point cloud) and one per MS band with
\code{--fast-orthophoto} (orthomosaic only, calibrated to reflectance via
the DLS camera+sun flag). All five resulting orthos are then
resampled onto the RGB ortho's grid and stacked into a single
7-band GeoTIFF (\verb{Red, Green, Blue, MS_G, MS_R, MS_RE, MS_NIR}) at
\verb{<project_dir>/<odm_dataset_subdir>/odm_orthophoto_dji.tif}.
}
\details{
Downstream \code{\link[=run_dronebio_workflow]{run_dronebio_workflow()}} / \code{\link[=read_multispectral_orthomosaic]{read_multispectral_orthomosaic()}}
auto-detects the 7-layer stack and uses \code{\link[=default_dji_mavic_3m_band_map]{default_dji_mavic_3m_band_map()}}
to expose Blue, Green, Red, RedEdge and NIR — Green/Red/RedEdge/NIR
are pulled from the calibrated MS bands, Blue from the RGB JPG
channel (the Mavic 3M does not capture a calibrated blue MS band).
}
\examples{
\dontrun{
  project <- dronebio_project("/path/to/flight",
                              images_subdir      = ".",
                              odm_dataset_subdir = "odm_dji_dataset",
                              odm_project_name   = "dji")
  project$images_dir <- "/path/to/raw/images"
  result <- run_odm_dji_mavic_3m(project)
  result$stacked_orthomosaic
}
}
