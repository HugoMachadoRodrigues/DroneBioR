% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field_caret.R
\name{caret_model_available}
\alias{caret_model_available}
\title{Check whether a caret model can be trained here}
\usage{
caret_model_available(
  method,
  installed = rownames(utils::installed.packages())
)
}
\arguments{
\item{method}{caret method id.}

\item{installed}{Character vector of installed package names.}
}
\value{
A list with \code{ok}, \code{missing} (packages) and \code{install_call} (a
ready-to-paste \code{install.packages()} line, \code{NA} when nothing is missing).
}
\description{
Must be called before every \code{\link[caret:train]{caret::train()}} call. caret's own install
check opens an interactive \code{menu()} prompt when a backend is missing,
which hangs a Shiny session started from an interactive R process.
}
\examples{
if (requireNamespace("caret", quietly = TRUE)) {
  caret_model_available("lm")$ok
}
}
