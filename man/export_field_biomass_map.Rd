% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field_caret.R
\name{export_field_biomass_map}
\alias{export_field_biomass_map}
\title{Export the exact full-resolution biomass map}
\usage{
export_field_biomass_map(
  model,
  reflectance,
  out_path,
  custom_index = NULL,
  chm = NULL,
  dsm = NULL,
  dtm = NULL,
  min_biomass = 0
)
}
\arguments{
\item{model}{A \code{dronebio_field_model}.}

\item{reflectance}{Native-resolution reflectance \code{SpatRaster}.}

\item{out_path}{GeoTIFF path to write.}

\item{custom_index}{Optional custom index \code{SpatRaster}.}

\item{chm, dsm, dtm}{Optional terrain \code{SpatRaster}s.}

\item{min_biomass}{Lower clamp for predictions (kg/ha).}
}
\value{
A single-layer \code{SpatRaster} named \code{biomass_kgha}, backed by
\code{out_path}.
}
\description{
Builds a native-resolution stack of the reflectance bands plus whatever
terrain layers the model uses, then predicts block by block through
\code{\link[=covariate_frame_from_pixels]{covariate_frame_from_pixels()}} - the same primitive extraction used - so
the exported surface is computed by identical arithmetic to training,
with no block-mean approximation. Streams straight to \code{out_path}.
}
