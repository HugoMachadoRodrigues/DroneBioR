% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/engine_webodm.R
\name{webodm_submit_task}
\alias{webodm_submit_task}
\title{Submit a task to a WebODM project}
\usage{
webodm_submit_task(
  base_url,
  token,
  project_id,
  image_paths,
  options = list(),
  name = NULL
)
}
\arguments{
\item{base_url}{Root URL of the WebODM server.}

\item{token}{JWT from \code{\link[=webodm_authenticate]{webodm_authenticate()}}.}

\item{project_id}{Integer project ID from \code{\link[=webodm_create_project]{webodm_create_project()}}.}

\item{image_paths}{Character vector of local image file paths to upload.}

\item{options}{A list of WebODM option \code{name = value} pairs. Use
\code{\link[=as_webodm_options]{as_webodm_options()}} to translate from \code{build_odm_args()}-style
arguments.}

\item{name}{Optional task name (default: \code{"DroneBioR task <timestamp>"}).}
}
\value{
Character task UUID.
}
\description{
Uploads the supplied image files as multipart form data alongside an
\code{options} JSON array (the WebODM format: \verb{[\{"name": "dsm", "value": true\}, ...]}). Returns the task id; processing happens
asynchronously on the WebODM server.
}
\examples{
\dontrun{
task_id <- webodm_submit_task(
  base_url, token, project_id,
  image_paths = list.files("/path/to/images", full.names = TRUE),
  options = as_webodm_options(camera_type = "rgb", build_dsm = TRUE)
)
}
}
