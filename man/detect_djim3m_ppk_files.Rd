% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dji_ppk.R
\name{detect_djim3m_ppk_files}
\alias{detect_djim3m_ppk_files}
\title{Detect DJI Mavic 3M PPK / RTK sidecar files}
\usage{
detect_djim3m_ppk_files(images_dir)
}
\arguments{
\item{images_dir}{Folder containing the raw DJI Mavic 3M images.}
}
\value{
A named list with character vectors \code{mrk}, \code{bin}, \code{nav}
(each empty when nothing matches). The \code{has_ppk_inputs} element
is \code{TRUE} when at least one \code{.bin} AND one \code{.nav} are present —
the minimum needed by an external PPK CLI.
}
\description{
Scans \code{images_dir} for the three families of DJI Mavic 3M sidecar
files: \verb{*_Timestamp.MRK} (trigger events + RTK positions),
\verb{*_PPKRAW.bin} (raw GNSS observables) and \verb{*_PPKNAV.nav} (broadcast
ephemerides). One mission produces one file per family; a folder
can hold several missions and therefore several files of each.
}
\examples{
\dontrun{
  files <- detect_djim3m_ppk_files("/path/to/ifasbahia10")
  files$mrk
}
}
