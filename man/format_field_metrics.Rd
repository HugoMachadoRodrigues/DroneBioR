% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field_caret.R
\name{format_field_metrics}
\alias{format_field_metrics}
\title{Format field model metrics for display}
\usage{
format_field_metrics(model, digits = 2)
}
\arguments{
\item{model}{A \code{dronebio_field_model}.}

\item{digits}{Decimal places.}
}
\value{
A data frame with \code{Split}, \code{n}, \code{R2}, \code{RMSE}, \code{MAE} and \code{RPIQ},
the numeric columns already formatted as strings.
}
\description{
The one place the two-decimal rounding happens, so the Shiny table, the
console \code{print()} and the \code{.txt} summary all show identical numbers.
}
\examples{
if (requireNamespace("caret", quietly = TRUE)) {
  set.seed(1)
  d <- data.frame(NDVI = runif(60, 0.2, 0.9))
  d$biomass_kgha <- 800 + 3000 * d$NDVI + rnorm(60, sd = 150)
  m <- fit_field_caret_model(d, predictors = "NDVI", method = "lm",
                             split = field_train_split(d, folds = 5))
  format_field_metrics(m)
}
}
