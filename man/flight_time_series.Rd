% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time_series.R
\name{flight_time_series}
\alias{flight_time_series}
\title{Compute a time series of a custom flight summary}
\usage{
flight_time_series(summary_fn, registry_path = default_flight_registry())
}
\arguments{
\item{summary_fn}{A function that takes a \code{dronebio_project} and
returns a single numeric value.}

\item{registry_path}{Path to the registry CSV.}
}
\value{
A data frame with columns \code{date}, \code{value}, \code{flight_id},
\code{project_dir}.
}
\description{
For each registered flight, opens the project at \code{flights$project_dir}
and applies \code{summary_fn(project)} to obtain a single numeric value.
Returns the values keyed by date so the result can be plotted directly
as a time series.
}
\details{
Errors thrown by \code{summary_fn} for individual flights surface as \code{NA}
values, so a partial registry (e.g. a few flights with missing
orthomosaics) still produces a usable plot.
}
\examples{
\dontrun{
# One row per flight, so register the projects first.
reg <- tempfile(fileext = ".csv")
register_flight("2026-04-01", "~/flights/2026-04-01", registry_path = reg)
register_flight("2026-05-01", "~/flights/2026-05-01", registry_path = reg)
flight_time_series(flight_ndvi_mean, registry_path = reg)
}
}
