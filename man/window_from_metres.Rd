% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field_extract.R
\name{window_from_metres}
\alias{window_from_metres}
\title{Convert a metric window to the nearest odd pixel window}
\usage{
window_from_metres(reference, window_m)
}
\arguments{
\item{reference}{A \code{terra::SpatRaster} whose resolution defines the
conversion.}

\item{window_m}{Desired support in metres (the side of the square).}
}
\value{
A list with \code{window} (odd pixel count), \code{window_m_actual} (what that
count spans) and \code{resolution}.
}
\description{
A window expressed in pixels changes physical meaning with the ground
sampling distance, so a quadrat-matched support has to be restated for every
survey. This converts metres to the nearest odd pixel count for a given
raster, and reports the metric support that count actually spans.
}
\examples{
r <- terra::rast(nrows = 100, ncols = 100, xmin = 0, xmax = 5.76, ymin = 0, ymax = 5.76)
window_from_metres(r, 0.52)
}
